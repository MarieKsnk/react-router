import React from 'react';

const HTML = () => {
  return (
    <div className='page'>
      <h1>Qu'est ce que le HTML ?</h1>
      <p>Le code HTML (HyperText Markup Language) est un langage de balisage standardisé qui permet de créer des pages web. Chaque page web est un document HTML, qui peut être soit une simple page texte, soit incorporer des éléments plus complexes comme des images, des liens, des formulaires, et même des scripts en langages comme Javascript ou PHP.</p>
    </div>
  );
}

export default HTML;